version: '3.8'
services:
  llm-app-examples:
    build:
      context: ./
      dockerfile: Dockerfile
      args:
        APP_VARIANT: ${APP_VARIANT}
    ports:
      - "8501:8501"
    env_file:
      - .env
    stdin_open: true
    tty: true
